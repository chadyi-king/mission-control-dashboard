name: Archived - Sanitize Dashboard Data Snapshot

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived workflow
        run: |
          echo 'This sanitizer workflow is intentionally archived.'
          echo 'Dashboard data must be generated from canonical state by the Hermes publish path, not rewritten by a separate GitHub action.'
