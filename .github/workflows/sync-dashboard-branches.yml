name: Deprecated Dashboard Branch Sync Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled publisher
        run: |
          echo 'This workflow is intentionally disabled.'
          echo 'Mission Control now deploys from main through .github/workflows/pages.yml.'
          echo 'Do not push main to gh-pages from this workflow.'
