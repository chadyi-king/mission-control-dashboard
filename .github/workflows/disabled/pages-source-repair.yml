name: Archived - Repair GitHub Pages Source

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived workflow
        run: |
          echo 'This repair workflow is intentionally archived.'
          echo 'Mission Control now has one publish path: validate main, enforce gh-pages / root, and publish main to gh-pages.'
          echo 'Use Publish Mission Control Dashboard instead.'
