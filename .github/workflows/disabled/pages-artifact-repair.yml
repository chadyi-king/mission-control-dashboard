name: Archived - Mission Control Pages Artifact Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived workflow
        run: |
          echo 'This repair workflow is intentionally archived.'
          echo 'Mission Control uses gh-pages / root as the single GitHub Pages publish path.'
          echo 'The active workflow verifies the gh-pages root source instead of deploying a competing Pages artifact.'
